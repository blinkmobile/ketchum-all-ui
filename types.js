/* @flow */

/* ::
import type { Map, OrderedSet, Set } from 'immutable'
*/

/* ::
export type Resource = Map<string, any>

export type CollectionIdMap = Map<string, Resource>
export type CollectionIds = OrderedSet<string>
export type CollectionSelectedIds = Set<string>
export type CollectionReducerState = Map<string, CollectionIdMap | CollectionIds | CollectionSelectedIds>

export type ReducerState = Map<string, any>
export type RootState = Map<string, ReducerState | CollectionReducerState>

type Selector = (state: RootState) => any
export type CollectionActions = { [id:string]: Function }
export type CollectionSelectors = { [id:string]: Selector }
*/
